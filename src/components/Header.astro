---
const currentUrl = Astro.url;
const pathname = currentUrl.pathname;
---

<header>
  <a href="#main-content" class="skip-link visually-hidden">Skip to Content</a>
  <a class="masthead" aria-label="Ally Crawler" href="/">A11y Edu</a>

  <nav class="site-nav" aria-label="Main Navigation">
    <ul>
      <li><a href="/" class={pathname === '/' ? 'current' : ''}>Report</a></li>
      <li><a href="/log" class={pathname.startsWith('/log') ? 'current' : ''}>Project Log</a></li>
      <li><a href="/poster" class={pathname.startsWith('/poster') ? 'current' : ''}>Poster</a></li>
      <li><a href="/todo" class={pathname.startsWith('/todo') ? 'current' : ''}>Todo</a></li>
    </ul>
  </nav>
</header>

<style>
  header {
    padding-inline: 3rem;
    padding-block: 1rem;
    border-bottom: 1px solid var(--color-body);
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    background-color: var(--color-background);
    z-index: 200;
    position: relative;
  }

  header * {
    line-height: 1;
  }

  .masthead {
    font-family: var(--font-sans);
    font-size: var(--size-step-1);
    text-decoration: none;
  }

  nav ul {
    display: flex;
    align-items: baseline;
    gap: 2rem;
    list-style-type: none;
    font-family: var(--font-sans);
  }

  nav ul li a {
    text-decoration: none;
  }

  nav ul li a:hover {
    text-decoration: underline;
  }

  .current {
    color: var(--color-primary);
    text-decoration: underline;
  }
</style>
